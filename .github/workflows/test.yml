name: CI Test
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run echo
        run: echo "Hello from GitHub Actions!"
      - name: Run date
        run: date
